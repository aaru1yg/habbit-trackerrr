/* Canonical Phase 2 interaction surface. CommandCenter remains the
   implementation name for backwards-compatible imports/tests; the shell
   exposes it as one Omni Panel for search, create, commands and coach. */
export { default } from '../layout/CommandCenter.jsx'
